// CSR only: Firebase Auth is client-side — same pattern as /analytics/ and /login/.
// The layout.svelte handles the admin check and redirect before any child content renders.
export const ssr = false;
